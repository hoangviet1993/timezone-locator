import { ConvertDateObjectPipe } from './convert-date-object.pipe';

describe('ConvertDateObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertDateObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
